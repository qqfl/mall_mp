<swiper class="banner"
        indicator-dots
        circular
        autoplay
        interval="3000"
        duration="500">
  <block wx:for="{{list}}" wx:key="index">
    <swiper-item>
      <image src="{{item.image}}" mode="widthFix"/>
    </swiper-item>
  </block>
</swiper>
